# Milestone 0: Bootstrap and Role-Aware Foundation

## Goal

Create a runnable repo foundation that matches the PRD stack and is ready for the first MVP slice.

## Completed in This Pass

1. root workspace structure
2. app boundaries for `super-app`, `admin-web`, and `api`
3. shared domain types for roles, orders, and artist states
4. role-aware app shell stub
5. health endpoint stub for the API
6. architecture blueprint for the next slice

## Remaining Before Milestone 0 Is Fully Closed

1. run `pnpm install`
2. run the apps and verify scripts
3. wire real router, state, and API clients
4. initialize git if this directory is intended to become the source repo

## Recommended Next Slice

`Milestone 1: Auth + Role Shell + Artist Onboarding Setup`

This should include:

1. user login and role session model
2. artist application form shell
3. admin review list shell
4. API module for artist application CRUD
